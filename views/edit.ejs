<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h3>Edit your chat</h3>
    <h2>Your id : <%= chat._id %></h2>
    <h2>This chat is sent from <%= chat.from %> to <%= chat.to %></h2>

    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
        <textarea name="msg" rows="10" cols="50"><%= chat.msg %></textarea>
        <br>
        <button>Edit</button>
    </form>

</body>
</html>
